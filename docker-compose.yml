version: "3"

services:

  api:
    container_name: nest-rest-api
    build: .
    # environment:
    #   - X=1
    ports:
      - 8081:3001
    volumes:
      - .:/home/app
    depends_on:
      - postgres
    env_file:
      - .env
  
  postgres:
    container_name: postgres
    image: postgres:14.5
    restart: always
    ports:
      - 5555:5432
    env_file:
      - .env
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
